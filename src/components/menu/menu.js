import React from "react";
import "./styles.css";

const Menu = ({ buttons }) => {
  return (
    <div>
      <button className="menuButtons">New Game</button>
    </div>
  );
};

export default Menu;
